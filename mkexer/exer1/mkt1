TARGET := display
SRC := $(TARGET).c
OBJ := $(TARGET).o
SOBJ := libdlink.so


CFLAGS := -g -Wall
LFLAGS := -lm -pthread
CC := gcc $(CFLAGS) $(LFLAGS)

all : $(SRC) $(OBJ) $(SOBJ)
.c.o:
	$(CC) -c $^
$(SOBJ):
	gcc -shared $(SRC) -fPIC -o $(SOBJ)

clean:
	rm -f $(TARGET) $(OBJ) $(SOBJ)

